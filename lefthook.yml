pre-commit:
  parallel: true
  commands:
    sidebar-links:
      run: .agents/tools/check-sidebar-links.sh
      skip:
        - merge
        - rebase
    frontmatter:
      run: .agents/tools/check-frontmatter.sh
      skip:
        - merge
        - rebase
